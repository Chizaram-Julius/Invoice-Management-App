# Invoice Management App
A fully responsive Invoice Management Application built with React and TailwindCSS based on the provided Figma design for HNG Stage 2 (Frontend).  The app enables users to create, view, update, and delete invoices, manage statuses (Draft, Pending, Paid), filter invoices, and toggle between light and dark themes. Data is persisted using LocalStorage.
